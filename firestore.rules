rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /antennas/{document=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
    match /alarms/{document=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
